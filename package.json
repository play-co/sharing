{
  "name": "devkit-sharing",
  "version": "0.1.0",
  "devkit": {
    "clientPaths": {
      "Sharing": "js"
    },
    "extensions": {
      "ios": "ios",
      "android": "android"
    }
  },
  "devDependencies": {
    "mocha": "^2.0.1",
    "jsio": "^2.0.0"
  },
  "scripts": {
    "test": "mocha --recursive -R spec -C test"
  }
}
